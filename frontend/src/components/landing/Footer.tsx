export function Footer() {
  return (
    <footer className="border-t border-line">
      <div className="mx-auto flex max-w-6xl flex-col gap-3 px-5 py-8 text-sm text-fg-muted sm:flex-row sm:items-center sm:justify-between">
        <p>AstraLedger · Razorpay AI Buildathon, Track 04</p>
        <p>Synthetic data only. Local-first. No external AI services.</p>
      </div>
    </footer>
  );
}
